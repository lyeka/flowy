# stores/
> L2 | 父级: src/CLAUDE.md

## 成员清单

gtd.js: GTD 核心状态管理，任务 CRUD + 列表筛选 + 持久化
calendar.js: 日历状态管理，日期分组 + 网格生成 + 导航
journal.js: 日记状态管理，一天一记约束 + 按日期分组 + 时间倒序

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
