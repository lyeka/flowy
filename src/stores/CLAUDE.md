# stores/
> L2 | 父级: src/CLAUDE.md

## 成员清单

gtd.js: GTD 核心状态管理，任务 CRUD + 列表筛选 + 持久化
calendar.js: 日历状态管理，日期分组 + 网格生成 + 导航
journal.js: 日记状态管理，一天一记约束 + 按日期分组 + 时间倒序
ai.js: AI 配置和状态管理，OpenAI API 集成 + 问题生成逻辑（支持流式输出回调）+ API Key 加密存储
editor.js: 编辑器样式配置管理，预设主题 + 自定义样式（bullet/标题大小/边框/字重）+ CSS 变量应用

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
